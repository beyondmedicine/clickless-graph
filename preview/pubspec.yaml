name: clickless_graph_preview
description: Preview app for clickless_graph.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  clickless_graph:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
