name: clickless_graph
description: "클릭리스 서비스 공용 그래프 UI 라이브러리"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.4

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/image/
